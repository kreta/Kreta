<form action="{{ url('kreta_web_issue_comment_new', {'issueId': issue.id}) }}" method="post" class="kreta-issue-comment-add">
    <img src="/images/gorka.png">
    <div class="textarea-container">
        {{ form_row(form.description) }}
        {{ form_end(form) }}
        <input type="submit" class="button tiny" value="Send">
    </div>
</form>
