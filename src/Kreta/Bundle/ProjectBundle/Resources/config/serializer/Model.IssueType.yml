# This file belongs to Kreta.
# The source code of application includes a LICENSE file
# with all information about license.
#
# @author benatespina <benatespina@gmail.com>
# @author gorkalaucirica <gorka.lauzirika@gmail.com>

Kreta\Component\Project\Model\IssueType:
    exclusion_policy: ALL
    relations:
        -
            rel: issue_types
            href:
                route: get_projects_issue_types
                parameters:
                    projectId: expr(object.getProject().getId())
                absolute: true
            exclusion:
                groups: [issueTypeList, issueType]
        -
            rel: project
            href:
                route: get_project
                parameters:
                    projectId: expr(object.getProject().getId())
                absolute: true
            exclusion:
                groups: [issueTypeList, issueType]
    properties:
        id:
            expose: true
            groups: [projectList, project, issueList, issue, issueTypeList, issueType]
        name:
            expose: true
            groups: [projectList, project, issueList, issue, issueTypeList, issueType]
